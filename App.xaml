<Application x:Class="CNCGui.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CNCGui"
             StartupUri="MainWindow.xaml"
             Startup="App_Startup">
    <Application.Resources>
        <!-- Side Panel Button Style -->
        <Style TargetType="Button" x:Key="SidePanelButton">
            <Setter Property="Background" Value="White" />
            <Setter Property="TextBlock.TextAlignment" Value="Center" />
            <Setter Property="Border.CornerRadius" Value="2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="SidePanelButtonBorder" CornerRadius="{TemplateBinding Border.CornerRadius}" BorderBrush="#FF314866" BorderThickness="1">
                            <Border.Background>
                                <LinearGradientBrush EndPoint="0.1,1" StartPoint="0.1,0">
                                    <GradientStop Color="#FFE1E6EC" Offset="1"/>
                                    <GradientStop Color="#B29FB3CB" Offset="0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter x:Name="SidePanelButtonPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="SidePanelButtonBorder">
                                    <Setter.Value>
                                        <LinearGradientBrush EndPoint="0.1,0" StartPoint="0.1,1">
                                            <GradientStop Color="#B29FB3CB" Offset="0"/>
                                            <GradientStop Color="#FFE1E6EC" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </Application.Resources>
</Application>
